services:
  - type: web
    name: vela-ai-voice
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: ~20
      - key: GEMINI_API_KEY
        sync: false
      - key: VAPI_ASSISTANT_ID
        sync: false
